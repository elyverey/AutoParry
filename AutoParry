local P=game:GetService("Players");local R=game:GetService("RunService");local W=game:GetService("Workspace");local V=game:GetService("VirtualInputManager");local U=game:GetService("UserInputService");local C=game:GetService("CollectionService");local LP=P.LocalPlayer;local PG=LP:WaitForChild("PlayerGui");local UI=loadstring(game:HttpGet("https://raw.githubusercontent.com/RillBoys/bolong.catui/refs/heads/main/b0lngUi.lua"))();local Win=UI:Window({Title="BOLONG-HUB",Image="84034353458936",Footer="Violence District",Author="",Color=Color3.fromRGB(255,255,255),Version="Testing",Search=true,Folder="BolongHub"});local Tab=Win:AddTab({Name="Exclusive",Icon="sparkles"});local AP={};local S={enabled=false,autoParryEnabled=false,radiusEsp=true,radius=12,aimStrictness=.1,parryDelay=0,lastParryTime=0,activeAttackers={},pendingParries={},pendingTimes={},pendingReadyTimes={},preArmTimes={},localCharacter=nil,localRoot=nil,cachedPing=0,lastPingUpdate=0,heartbeat=nil,characterConnection=nil,trackState={}};local REACH,LUNGE,CORR,LCOR=3,1.25,1.25,1.45;local PREARM_TIMEOUT=.35;local PREARM_RANGE=4.75;local N={};for _,i in ipairs({"78432063483146","121216847022485","74968262036854","132817836308238","82666958311998","111920872708571","106871536134254","109402730355822","130593238885843","138720291317243","139369275981139","133963973694098","78935059863801","88848807662765"})do N[i]=1 end;local L={};for _,i in ipairs({"118907603246885","135002183282873","113255068724446","129784271201071","105374834496520","117070354890871","115244153053858","110355011987939","117042998468241","122812055447896"})do L[i]=1 end;local VALID={};for i in pairs(N)do VALID[i]="attack"end;for i in pairs(L)do VALID[i]="lungehold"end;local RP,RA,RB={}, {},{};local AC,PC={},{};local function root(c)return c and(c:FindFirstChild("HumanoidRootPart")or c:FindFirstChild("UpperTorso")or c:FindFirstChild("Torso"))end;local function flat(v)v=Vector3.new(v.X,0,v.Z);return v.Magnitude>.001 and v.Unit or nil end;local function updateChar()local c=LP.Character;if c~=S.localCharacter then S.localCharacter=c;S.localRoot=root(c)elseif c and(not S.localRoot or not S.localRoot.Parent)then S.localRoot=root(c)end end;local function threshold()local x=math.clamp(tonumber(S.aimStrictness)or .1,.1,.7);if x<=.1 then return math.cos(math.rad(93))elseif x<=.3 then return math.cos(math.rad(93-((x-.1)/.2)*10))elseif x<=.5 then return math.cos(math.rad(83-((x-.3)/.2)*12))elseif x<=.7 then return math.cos(math.rad(71-((x-.5)/.2)*12))end;return math.cos(math.rad(59))end;local function reach(t)return t=="lungehold"and REACH+LUNGE or REACH end;local function corridor(t)return t=="lungehold"and LCOR or CORR end;local function attackDir(r)if not r then return end;local v=r.AssemblyLinearVelocity;local h=Vector3.new(v.X,0,v.Z);local d=flat(h);return d and h.Magnitude>=4 and d or flat(r.CFrame.LookVector)end;local function linePoint(o,d,p,r)local x=math.clamp((p-o):Dot(d),0,r);return o+d*x,x end;local function checkPath(r,t,pos)local d=attackDir(r);if not d or not pos then return false end;local p,a=linePoint(r.Position,d,pos,reach(t));local miss=(pos-p).Magnitude;local tr=root(S.localCharacter);local size=tr and math.clamp(math.max(tr.Size.X,tr.Size.Z)*.65,.9,1.5)or 1.25;if a<=0 or a>reach(t)or miss>corridor(t)+size then return false end;local td=flat(pos-r.Position);if not td then return false end;return d:Dot(td)>=threshold()-.2,miss,a,d end;local function checkAim(r,t,dist)if not r or not S.localRoot then return false end;local ad=attackDir(r);local td=flat(S.localRoot.Position-r.Position);if not ad or not td then return false end;local dot=ad:Dot(td);if dist<=1.25 then return dot>=.15 end;if dist<=reach(t)+.75 then return dot>=-.25 end;if dot>=threshold()then return true end;return checkPath(r,t,S.localRoot.Position)==true end;local function updatePing()local n=os.clock();if n-S.lastPingUpdate<.2 then return end;S.lastPingUpdate=n;local ok,p=pcall(function()return LP:GetNetworkPing()end);if ok and typeof(p)=="number"then S.cachedPing=math.clamp(p,0,1)end end;local function los(k,t)if not S.localRoot or not t then return false end;local pts={};for _,n in ipairs({"HumanoidRootPart","UpperTorso","LowerTorso","Head"})do local p=t:FindFirstChild(n);if p then pts[#pts+1]=p.Position end end;if #pts==0 then return false end;local q=RaycastParams.new();q.FilterType=Enum.RaycastFilterType.Exclude;q.FilterDescendantsInstances={k,t};for _,p in ipairs(pts)do if not W:Raycast(S.localRoot.Position,p-S.localRoot.Position,q)then return true end end;return false end;local function cooldown(d)if d<=2.5 then return .025 elseif d<=3.5 then return .03 elseif d<=4.5 then return .035 elseif d<=5.5 then return .045 elseif d<=7 then return .055 end;return .075 end;local function doParry()if not S.autoParryEnabled then return false end;local now=os.clock();if now-S.lastParryTime<.055 then return false end;local c=LP.Character;if not c then return false end;local g=PG:FindFirstChild("Survivor-mob");local co=g and g:FindFirstChild("Controls");local b=co and co:FindFirstChild("Gui-mob");if b and b:IsA("ImageButton")then local ok=pcall(function()firesignal(b.MouseButton1Down);task.delay(.05,function()if b and b.Parent then pcall(function()firesignal(b.MouseButton1Up)end)end end)end);if not ok then return false end;S.lastParryTime=now else local ok,input=pcall(function()local i=Instance.new("InputObject");i.UserInputType=Enum.UserInputType.MouseButton2;i.UserInputState=Enum.UserInputState.Begin;return i end);if ok and input then pcall(function()for _,con in getconnections(U.InputBegan)do con:Fire(input,false)end end)else pcall(function()V:SendMouseButtonEvent(0,0,1,true,game,0)end)end;S.lastParryTime=now end;local r=c:FindFirstChild("HumanoidRootPart");if r then task.defer(function()pcall(function()if C:HasTag(r,"doing action")then C:RemoveTag(r,"doing action")end end)end)end;return true end;local function clearPending(p)S.pendingParries[p]=nil;S.pendingTimes[p]=nil;S.pendingReadyTimes[p]=nil;S.preArmTimes[p]=nil end;local function register(p,tr)if not p or p==LP or not tr or not tr.Animation then return end;local id=tostring(tr.Animation.AnimationId or ""):gsub("rbxassetid://","");local typ=VALID[id];if not typ then return end;local old=S.activeAttackers[p];local st=S.trackState[tr];local pos=0;pcall(function()pos=tr.TimePosition end);if st then st.lastPosition=pos;st.cycle=st.cycle+1;st.registeredCycle=0;st.lastRegister=os.clock()else st={lastPosition=pos,cycle=1,registeredCycle=0,lastRegister=os.clock()};S.trackState[tr]=st end;local cyc=st.cycle;S.activeAttackers[p]={attackType=typ,time=os.clock(),track=tr,animationId=id,cycle=cyc,token=(old and old.token or 0)+1};clearPending(p)end;local function scanPlayingTracks(p,h)if not p or p==LP or not h then return end;local ok,trks=pcall(function()return h:GetPlayingAnimationTracks()end);if not ok or not trks then return end;local active={};for _,tr in ipairs(trks)do if tr and tr.Animation then local id=tostring(tr.Animation.AnimationId or ""):gsub("rbxassetid://","");local typ=VALID[id];if typ then active[tr]=true;local st=S.trackState[tr];local pos=0;local playing=true;pcall(function()pos=tr.TimePosition;playing=tr.IsPlaying end);if playing then if not st then st={lastPosition=pos,cycle=1,registeredCycle=0,lastRegister=os.clock()};S.trackState[tr]=st end;local reset=pos+.12<st.lastPosition;if reset then st.cycle=st.cycle+1;st.registeredCycle=0;st.lastRegister=os.clock()end;if st.registeredCycle~=st.cycle then local data=S.activeAttackers[p];if not data or data.track~=tr or data.cycle~=st.cycle then S.activeAttackers[p]={attackType=typ,time=os.clock(),track=tr,animationId=id,cycle=st.cycle,token=(data and data.token or 0)+1};clearPending(p)end;st.registeredCycle=st.cycle;st.lastRegister=os.clock()end;st.lastPosition=pos end end end;for tr,st in pairs(S.trackState)do if typeof(tr)=="Instance" and tr:IsA("AnimationTrack")then if not active[tr]then local playing=false;pcall(function()playing=tr.IsPlaying end);if not playing then local data=S.activeAttackers[p];if data and data.track==tr and os.clock()-data.time>.12 then S.activeAttackers[p]=nil;clearPending(p)end;S.trackState[tr]=nil end end else S.trackState[tr]=nil end end end;local function unwatchChar(p)if AC[p]then for _,c in ipairs(AC[p])do pcall(function()c:Disconnect()end)end end;AC[p]=nil;S.activeAttackers[p]=nil;clearPending(p);for tr in pairs(S.trackState)do if typeof(tr)=="Instance" and tr:IsA("AnimationTrack")then local par=nil;pcall(function()par=tr.Parent end);if not par then S.trackState[tr]=nil end end end end;local function watchChar(p,c)unwatchChar(p);if not c then return end;local h=c:FindFirstChildOfClass("Humanoid")or c:WaitForChild("Humanoid",5);if not h then return end;AC[p]={h.AnimationPlayed:Connect(function(t)register(p,t)end)};task.spawn(function()for _=1,15 do if not c.Parent then break end;scanPlayingTracks(p,h);task.wait(.02)end end)end;local function watch(p)if not p or p==LP or PC[p]then return end;PC[p]={p.CharacterAdded:Connect(function(c)watchChar(p,c)end)};if p.Character then task.spawn(function()watchChar(p,p.Character)end)end end;local function unwatch(p)if PC[p]then for _,c in ipairs(PC[p])do pcall(function()c:Disconnect()end)end end;PC[p]=nil;unwatchChar(p)end;local function execute(p,t,tr,token)if not S.enabled or not S.autoParryEnabled then clearPending(p);return end;local c=p.Character;local kr=c and root(c);if not kr or not S.localRoot then clearPending(p);return end;local data=S.activeAttackers[p];if not data or data.attackType~=t then clearPending(p);return end;if token and data.token~=token then clearPending(p);return end;if tr and data.track~=tr then clearPending(p);return end;local age=os.clock()-data.time;if age<0 or age>(t=="lungehold"and 1.05 or .8)then S.activeAttackers[p]=nil;clearPending(p);return end;if doParry()then S.activeAttackers[p]=nil end;clearPending(p)end;local function validate(p,t,tr)if not S.enabled or not S.autoParryEnabled or not S.localRoot then return end;local now=os.clock();if S.pendingParries[p]then local ready=S.pendingReadyTimes[p];if ready and now<ready then return end;if not ready then local pt=S.pendingTimes[p]or now;if now-pt<.055 then return end end;clearPending(p)end;local c=p.Character;local kr=c and root(c);local data=S.activeAttackers[p];if not c or not kr or not data or data.attackType~=t then return end;if tr and data.track~=tr then return end;local age=now-data.time;if age<0 or age>(t=="lungehold"and 1.05 or .8)then S.activeAttackers[p]=nil;clearPending(p);return end;if tr then local ok,playing=pcall(function()return tr.IsPlaying end);if ok and not playing and age>.1 then S.activeAttackers[p]=nil;clearPending(p);return end end;local pos=S.localRoot.Position;local offset=pos-kr.Position;local dist=offset.Magnitude;if dist>math.max(0,S.radius)then return end;local hit,miss,along,dir=checkPath(kr,t,pos);if not hit or not checkAim(kr,t,dist)or not los(c,S.localCharacter)then return end;local toTarget=flat(offset);if not toTarget then return end;if dist<=1.25 then local ad=attackDir(kr);if not ad or ad:Dot(toTarget)<.15 then return end end;local v=kr.AssemblyLinearVelocity;local hv=Vector3.new(v.X,0,v.Z);local closing=hv:Dot(toTarget);local ping=math.clamp(S.cachedPing*.65,0,.22);local mt=closing>=18 and .09 or closing>=14 and .075 or closing>=10 and .06 or closing>=6 and .05 or .035;local pt=ping+mt;local lv=S.localRoot.AssemblyLinearVelocity;local lhv=Vector3.new(lv.X,0,lv.Z);local predicted=pos+lhv*pt;local pp,pa=linePoint(kr.Position,dir,predicted,reach(t));local pm=(predicted-pp).Magnitude;local tradius=root(S.localCharacter);local size=tradius and math.clamp(math.max(tradius.Size.X,tradius.Size.Z)*.65,.9,1.5)or 1.25;if pa<=0 or pa>reach(t)or pm>corridor(t)+size then return end;if closing<=0 and dist>reach(t)then return end;local cd=cooldown(pm);if now-S.lastParryTime<cd then S.pendingParries[p]=true;S.pendingTimes[p]=now;S.pendingReadyTimes[p]=S.lastParryTime+cd;return end;S.pendingParries[p]=true;S.pendingTimes[p]=now;S.pendingReadyTimes[p]=now;local delay=math.clamp(tonumber(S.parryDelay)or 0,0,.1);local token=data.token;local cycle=data.cycle;if delay<=0 then execute(p,t,tr,token)else task.delay(delay,function()local current=S.activeAttackers[p];if S.pendingParries[p]and current and current.cycle==cycle then execute(p,t,tr,token)else clearPending(p)end end)end end;local function preArm(p,d)if not S.enabled or not S.autoParryEnabled or not S.localRoot or not p or not p.Parent or not d or S.pendingParries[p]then return end;local c=p.Character;local kr=c and root(c);if not c or not kr then return end;local age=os.clock()-d.time;if age<0 or age>(d.attackType=="lungehold"and 1.05 or .8)then return end;local offset=S.localRoot.Position-kr.Position;local dist=offset.Magnitude;if dist>math.min(math.max(0,S.radius),PREARM_RANGE)then return end;local toTarget=flat(offset);if not toTarget then return end;local v=kr.AssemblyLinearVelocity;local hv=Vector3.new(v.X,0,v.Z);if hv:Dot(toTarget)<=0 then return end;local now=os.clock();local cd=cooldown(dist);S.pendingParries[p]=true;S.pendingTimes[p]=now;S.preArmTimes[p]=now;S.pendingReadyTimes[p]=math.max(now,S.lastParryTime+cd);local token,cycle=d.token,d.cycle;task.delay(math.max(0,S.pendingReadyTimes[p]-now),function()if not S.enabled or not S.autoParryEnabled then clearPending(p);return end;local current=S.activeAttackers[p];if not S.pendingParries[p]or not current or current.token~=token or current.cycle~=cycle then clearPending(p);return end;local started=S.preArmTimes[p];if not started or os.clock()-started>PREARM_TIMEOUT then clearPending(p);return end;validate(p,current.attackType,current.track)end)end;local function retryPending()if not S.enabled or not S.autoParryEnabled then return end;local now=os.clock();for p in pairs(S.pendingParries)do local d=S.activeAttackers[p];local ready=S.pendingReadyTimes[p];local started=S.preArmTimes[p]or S.pendingTimes[p];if not p.Parent or not d or not ready or not started or now-started>PREARM_TIMEOUT then clearPending(p)elseif now>=ready then validate(p,d.attackType,d.track)end end end;local function cleanup()local n=os.clock();for p,d in pairs(S.activeAttackers)do if not p.Parent or not p.Character or n-d.time>(d.attackType=="lungehold"and 1.05 or .8)then S.activeAttackers[p]=nil;clearPending(p)end end;for p,t in pairs(S.pendingTimes)do local started=S.preArmTimes[p]or t;if not p.Parent or n-started>PREARM_TIMEOUT then clearPending(p)end end end;local function process()if not S.enabled or not S.autoParryEnabled then return end;for p in pairs(PC)do if p and p.Parent and p~=LP then local c=p.Character;local h=c and c:FindFirstChildOfClass("Humanoid");if h then scanPlayingTracks(p,h)end end end;for p,d in pairs(S.activeAttackers)do if d and p.Parent then preArm(p,d)end end;retryPending();for p,d in pairs(S.activeAttackers)do if d and p.Parent then validate(p,d.attackType,d.track)end end end;local function createESP()if #RA>0 then return end;local h=Instance.new("Part");h.Name="BOLONG_AutoParryRadius";h.Anchored=true;h.CanCollide=false;h.CanTouch=false;h.CanQuery=false;h.Transparency=1;h.Size=Vector3.new(.1,.1,.1);h.Parent=W;RP[1]=h;for i=1,72 do RA[i]=Instance.new("Attachment",h)end;for i=1,72 do local b=Instance.new("Beam",h);b.Attachment0=RA[i];b.Attachment1=RA[i%72+1];b.FaceCamera=true;b.Segments=1;b.Width0=.045;b.Width1=.045;b.Transparency=NumberSequence.new(.35);RB[i]=b end end;local function updateESP()if not S.radiusEsp or not S.localRoot then for _,b in ipairs(RB)do b.Enabled=false end;return end;createESP();local h=RP[1];h.CFrame=CFrame.new(S.localRoot.Position);for i,a in ipairs(RA)do local x=(i-1)/#RA*math.pi*2;a.Position=Vector3.new(math.cos(x)*S.radius,0,math.sin(x)*S.radius)end;for _,b in ipairs(RB)do b.Enabled=true end end;local function clearESP()for _,x in ipairs(RB)do pcall(function()x:Destroy()end)end;for _,x in ipairs(RA)do pcall(function()x:Destroy()end)end;for _,x in ipairs(RP)do pcall(function()x:Destroy()end)end;table.clear(RB);table.clear(RA);table.clear(RP)end;function AP:SetEnabled(v)S.enabled=v==true;S.autoParryEnabled=v==true;if not S.enabled then table.clear(S.activeAttackers);table.clear(S.pendingParries);table.clear(S.pendingTimes);table.clear(S.pendingReadyTimes);table.clear(S.preArmTimes);table.clear(S.trackState)end end;function AP:SetRadius(v)local n=tonumber(v);if n then S.radius=math.clamp(n,4,40)end end;function AP:SetAimStrictness(v)local n=tonumber(v);if n then S.aimStrictness=math.clamp(n,.1,.7)end end;function AP:SetParryDelay(v)local n=tonumber(v);if n then S.parryDelay=math.clamp(math.round(n*100)/100,0,.1)end end;function AP:SetRadiusESP(v)S.radiusEsp=v==true;if not S.radiusEsp then for _,b in ipairs(RB)do b.Enabled=false end end end;function AP:Destroy()S.enabled=false;S.autoParryEnabled=false;if S.heartbeat then pcall(function()S.heartbeat:Disconnect()end)end;if S.characterConnection then pcall(function()S.characterConnection:Disconnect()end)end;if AP._playerAddedConnection then pcall(function()AP._playerAddedConnection:Disconnect()end)end;if AP._playerRemovingConnection then pcall(function()AP._playerRemovingConnection:Disconnect()end)end;for p in pairs(PC)do unwatch(p)end;table.clear(PC);table.clear(AC);table.clear(S.activeAttackers);table.clear(S.pendingParries);table.clear(S.pendingTimes);table.clear(S.pendingReadyTimes);table.clear(S.preArmTimes);table.clear(S.trackState);clearESP()end;updateChar();for _,p in ipairs(P:GetPlayers())do watch(p)end;AP._playerAddedConnection=P.PlayerAdded:Connect(watch);AP._playerRemovingConnection=P.PlayerRemoving:Connect(unwatch);S.characterConnection=LP.CharacterAdded:Connect(function(c)S.localCharacter=c;S.localRoot=root(c);task.spawn(function()if not S.localRoot then S.localRoot=c:WaitForChild("HumanoidRootPart",5)end end)end);S.heartbeat=R.Heartbeat:Connect(function()updateChar();updatePing();updateESP();cleanup();process()end);local Sec=Tab:AddSection("Auto Parry",false);local HS=Sec:AddHStack({Padding=6,Height=46,Sizing="Equal"});HS:AddToggle({Title="Auto Parry",Default=false,Callback=function(v)AP:SetEnabled(v)end});HS:AddToggle({Title="Radius ESP",Default=true,Callback=function(v)AP:SetRadiusESP(v)end});Sec:AddSlider({Title="Detection Range",Min=4,Max=40,Default=12,Increment=1,Callback=function(v)AP:SetRadius(v)end});Sec:AddSlider({Title="Aim Strictness",Min=.10,Max=.70,Default=.10,Increment=.01,Callback=function(v)AP:SetAimStrictness(v)end});Sec:AddSlider({Title="Auto Parry Delay (s)",Min=0,Max=.10,Default=0,Increment=.01,Callback=function(v)AP:SetParryDelay(v)end});AP:SetRadius(12);AP:SetAimStrictness(.1);AP:SetParryDelay(0);AP:SetRadiusESP(true);return AP
